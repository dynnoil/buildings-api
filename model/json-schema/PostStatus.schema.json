{
    "bsonType": "object",
    "required": [
        "name",
        "description"
    ],
    "properties": {
        "name": {
            "bsonType": "string",
            "maxLength": 140,
            "description": "A name of a status"
        },
        "description": {
            "bsonType": "string",
            "maxLength": 280,
            "description": "A short description of a status"
        }
    }
}